PORT=3001
NODE_ENV=development

# Leave DATABASE_URL blank locally — the backend falls back to a SQLite file
# (backend/data.sqlite). On Render, this is injected automatically and points
# to the managed Postgres instance.
# DATABASE_URL=
