# Backend base URL. Leave unset locally — the Vite dev proxy forwards /api/* to
# http://127.0.0.1:8000 so the frontend always calls /api regardless of env.
# Set this for production deploys (e.g. Render) so the built bundle hits the
# backend service directly:
# VITE_API_URL=https://your-backend.onrender.com
